import { Meta } from '@storybook/addon-docs'

<Meta title="Getting Start/Installation" />

# Установка

Для начала вам необходимо установить Wuki в свое React-приложение. Это можно сделать с помощью Npm:

```
npm install wuki
```

или Yarn:

```
yarn add wuki
```

## Провайдер

Провайдер передаёт [глобальный файл конфигурации](?path=/docs/getting-start-theming--page) всем компонентам, так же внутри используется [normalize.css](https://necolas.github.io/normalize.css/). Провайдер необходим для корректной работы UI библиотеки.

```
import { WukiProvider } from 'wuki';
```

Объявите Провайдер в вашем приложении на глобальном уровне:

```
ReactDOM.render(
  <WukiProvider>
    <App />
  </WukiProvider>,
  document.getElementById('root')
);
```

> 🚨 Изначально Провайдер будет использовать стандартную конфигурацию Wuki UI, если вы хотите [изменить](?path=/docs/getting-start-theming--page) это и написать свою конфигурацию, то блаблабла

## Компоненты

После успешной интеграции Provider вы можете использовать любой компонент в любом месте вашего приложения без каких либо ограничений. Для этого просто импортируйте компонент из Wuki:

```
import { Text } from 'wuki'
```