import { Meta } from '@storybook/addon-docs'

<Meta title="Getting Start/Installation" />

## Установка

> Рассказать о том что нужно установить и о том что нужно сделать для успешного начала работы.<br />
> Автору желательно писать руководство при создании нового проекта.

Для начала вам необходимо установить Wuki в свое React-приложение. Это можно сделать с помощью Npm:

```
npm install wuki
```

или Yarn:

```
yarn add wuki
```

### Провайдер

Затем нам необходимо объявить Провайдер для всего приложения. Он необходим для передачи глобальных настроек и темы в компоненты.

```
import { WukiProvider } from 'wuki';
```

И внедрить его в ваше приложение на глобальном уровне

```
ReactDOM.render(
  <WukiProvider>
    <App />
  </WukiProvider>,
  document.getElementById('root')
);
```

> Изначально Провайдер будет использовать стандартную конфигурацию Wuki UI, если вы хотите изменить это и написать свою конфигурацию, то блаблабла 🚨

### Компоненты

После успешной интеграции Provider вы можете использовать любой компонент в любом месте вашего приложения без каких лобо ограничений. Для этого просто импортируйте компонент из Wuki:

```
import { Text } from 'wuki'
```