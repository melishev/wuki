import { useTranslation } from 'react-i18next'
import DocsNote from '../../../.storybook/components/docsNote'

export const t = (key) => useTranslation('code').t(key);

<DocsNote status={t('note.title')}>{t('note.text')} <a href="#">{t('note.button')}</a>.</DocsNote>