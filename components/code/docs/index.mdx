import {
  Title,
  Source,
  Story,
  Stories,
  DocsStory,
  ArgsTable
} from '@storybook/addon-docs';

import { Code } from '../../index';

import { useTranslation } from 'react-i18next'

![image](https://paradigm.mail.ru/notionStatic/938bf97d-d8f9-48cb-ba4d-9fe4c6c027de.png)

<br />

<Title />

Компонент `<Code />` позволяет отобразить исходный код. 

<Code code="import { Code } from 'wuki';" />

<h2>{useTranslation('text').t('subTitle')}</h2>

<p>{useTranslation('text').t('test')}</p>

#### 1. Google Fonts

<Code code={`<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Manrope:wght@200;300;400;500;600;700;800&display=swap"
/>`} />

#### 2. NPM

<Code code="npm install @fontsource/manrope" />

<p>{useTranslation('text').t('test2')}</p>

<Code code="import '@fontsource/manrope/200.css';
import '@fontsource/manrope/300.css';
import '@fontsource/manrope/400.css';
import '@fontsource/manrope/500.css';
import '@fontsource/manrope/600.css';
import '@fontsource/manrope/700.css';
import '@fontsource/manrope/800.css';" />

<p>{useTranslation('text').t('test3')} <a href="https://fontsource.org/fonts/manrope">Fontsource</a></p>

<h2>Примеры</h2>
<br/>

<h3>Default</h3>
<p>По умолчанию вам не нужно передавать какие либо параметры кроме `code` в этот компонент. Он прекрасен и удобен уже из коробки. {`<Code />`} будет автоматически подстравиваться под передаваемые данные.</p>
<p>В одну строку:</p>
<DocsStory id="components-code--single-line" />
<p>В несколько строк:</p>
<DocsStory id="components-code--multi-line" />

<h3>Inline</h3>
<p>Компонент `Code` может использоваться как вставка в текст, для этого необходимо передать компоненту параметр `inline`</p>
<DocsStory id="components-code--inline" />

## Component API (props)

<ArgsTable />