import {
  Title,
  Description,
  Primary,
  Source,
  ArgsTable
} from '@storybook/addon-docs';

![image](https://paradigm.mail.ru/notionStatic/938bf97d-d8f9-48cb-ba4d-9fe4c6c027de.png)

<br />

<Title />

<!-- This is the foundation of the entire application. The grid allows you to correctly position all elements on the screen of any device. -->
Это основа всего приложения. Сетка позволяет правильно расположить все элементы на экране любого устройства.

```
import { Grid } from 'wuki';
```

## Основная информация - информация присущая данному компоненту

### Должна импортироваться

<!-- ## Entities -->
## Сущности

<!-- Grid contains two entities:
  1. Container - defines a columnar system built on CSS Grid, which is many times more convenient and easier to use than CSS FlexBox. -->
Grid содержит две сущности:
  1. Контейнер - определяет систему столбцов, построенную на CSS Grid, которая во много раз удобнее и проще в использовании, чем CSS FlexBox.
```
<Grid container>
</Grid>
```
<!-- 2. Element - is a column in a column system. -->
  2. Элемент - столбец в системе столбцов.
```
<Grid col={12}>
</Grid>
```
<!-- In standard situations, you should stick to as few `<Grid container>` as possible. One container is enough to create standard page layout and cover all needs. -->
В стандартных ситуациях вам следует придерживаться как можно меньшего числа `<Grid container>`. Одного контейнера достаточно, чтобы создать стандартный макет страницы и закрыть все потребности.

## Адаптивность

## Примеры - должны идти стандартом

## Component

<Primary />

## Examples

<Primary />

## Component API (props)

Благодаря простому API(props) вы можете настроить компонент под ваши нужды. Чаще всего в этом не будет нужды, но для лучшего эффекта они и существуют
<ArgsTable />